# Resume Description

Built an enterprise multi-tool AI Agent platform foundation using FastAPI, PostgreSQL + pgvector, Redis, Celery, JWT and RBAC. Implemented Alembic-managed schema design for knowledge bases, documents, agent runs, tracing, tool calls, approvals, audit logs and SQL query logs, plus Docker Compose local deployment and provider abstraction with Mock LLM/Embedding fallback.

